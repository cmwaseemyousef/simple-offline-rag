<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="600" viewBox="0 0 1200 600">
  <defs>
    <style>
      .bg { fill: #0d1117; }
      .panel { fill: #161b22; stroke: #30363d; stroke-width: 2; rx: 12; }
      .title { fill: #c9d1d9; font: 700 20px 'Cascadia Mono', 'Fira Code', Consolas, monospace; }
      .mono { fill: #c9d1d9; font: 16px 'Cascadia Mono', 'Fira Code', Consolas, monospace; }
      .accent { fill: #7ee787; }
      .muted { fill: #8b949e; }
      .rule { stroke: #30363d; stroke-width: 1; }
    </style>
  </defs>

  <rect class="bg" x="0" y="0" width="1200" height="600"/>
  <rect class="panel" x="40" y="40" width="1120" height="520"/>

  <text class="title" x="60" y="80">$ python -m src.app.cli "What is RAG and why use it?" --k 3 --provider offline</text>
  <line class="rule" x1="60" y1="100" x2="1140" y2="100"/>

  <text class="mono accent" x="60" y="140">─ RAG Answer ─────────────────────────────────────────────────────────────────────────────────────────────────────────</text>

  <text class="mono" x="60" y="180">Answer (offline):</text>
  <text class="mono" x="80" y="210">- RAG grounds answers in retrieved documents to reduce hallucinations. [intro_rag.txt#0]</text>
  <text class="mono" x="80" y="240">- TF-IDF is a strong offline baseline for quick local retrieval. [tfidf_baseline.txt#0]</text>
  <text class="mono muted" x="80" y="270">(composed from retrieved local text with citations)</text>

  <text class="mono accent" x="60" y="310">Top Sources</text>
  <text class="mono" x="60" y="340">1. tfidf_baseline.txt  | chunk 0 | score 0.175 | RAG is often implemented with embeddings and a vector database...</text>
  <text class="mono" x="60" y="370">2. intro_rag.txt       | chunk 0 | score 0.108 | A Retrieval-Augmented Generation (RAG) system grounds an LLM’s...</text>
  <text class="mono" x="60" y="400">3. intro_rag.txt       | chunk 1 | score 0.000 | ...semantic similarity. FAQ: Do I need an API key? No—offline...</text>

  <text class="mono muted" x="60" y="460">simple-offline-rag • Offline-first RAG demo</text>
</svg>
